import { MenuCardTypes } from '../../molecules/MenuCard/types';

export interface PropsTypes {
  menus?: MenuCardTypes[];
  width?: number;
  height?: number;
  backgroundColor?: string;
  cardColor?: string;
}
