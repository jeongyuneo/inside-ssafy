import { PropTypes as NavbarPropTypes } from '../../molecules/Navbar/types';
import { PropTypes as BusInfoNavigatorPropTypes } from '../../molecules/BusInfoNavigator/types';
import { PropTypes as BusInfoNavbarPropTypes } from '../../molecules/BusInfoNavbar/types';

export interface PropTypes
  extends NavbarPropTypes,
    BusInfoNavigatorPropTypes,
    BusInfoNavbarPropTypes {}
